name: Docker Run

on:
  workflow_dispatch:

jobs:
  run-container:
    runs-on: self-hosted

    steps:
      - name: Show Docker Images
        run: docker images

      - name: Run JMeter Container
        run: docker run --rm jmeter-demo:v1
